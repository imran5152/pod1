apiVersion: v1
kind: Service

metadata:
  name: medical-svc

spec:
  type: NodePort
  selector:
    app: medical-pod

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080   
